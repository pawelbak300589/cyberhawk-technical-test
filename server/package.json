{
  "name": "cyberhawk-technical-test-server",
  "version": "1.0.0",
  "description": "Cyberhawk Technical Test API",
  "main": "src/server.js",
  "scripts": {
    "watch": "nodemon src/server.js --legacy-watch",
    "start": "set PORT=8000&& node src/server.js",
    "db:migrate": "npx sequelize-cli db:migrate",
    "db:migrate:undo": "npx sequelize-cli db:migrate:undo:all",
    "db:seed": "npx sequelize-cli db:seed:all",
    "db:seed:undo": "npx sequelize-cli db:seed:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.21.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.3.0",
    "nodemon": "^2.0.18",
    "sequelize-cli": "^6.4.1"
  }
}
